# railwayc

Credential harness around the official `railway` CLI — resolves a project-scoped Railway token, injects it, and forwards your command straight to `railway` (everything but `help` and `doctor` passes through untouched). Run `railwayc help`; for the command surface run `railway help`.
