# stripe

Read-only Stripe CLI over the REST API — emit a neutral JSON contract of account activity (`sync-plan`), check identity/reachability (`doctor`), list invoices and download their PDFs, and read balance-transactions and payouts. Run `stripe help`.
