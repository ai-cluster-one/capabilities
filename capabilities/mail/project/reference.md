# Mail — reference

> **Purpose.** The standing home for *project-specific* operational context: prose about how **this project** uses mail, where that's neither a value nor the command surface. What a mailbox means here, a drafting convention, a routing quirk in this project's mail, the meaning behind an account selector.
>
> Structural values (accounts, mailboxes, export paths) live in `identifiers.md`; how the CLI works — the command surface, the `<account>` matching rule, the read/draft/no-send boundary, the JXA/Automation model — lives in `mail --help` (project-agnostic, never copied here).
>
> Ships empty on purpose, so the home is always labeled and no agent has to decide whether it should exist or what goes in it. Populate it only when real context accrues — an empty reference is conformant, not a gap. Replace this note with that content when it arrives.
