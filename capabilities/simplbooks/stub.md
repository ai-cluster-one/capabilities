# simplbooks

SimpleBooks accounting CLI over a browser session (no public API) — read clients/invoices/expenses/accounts/bank-transactions/kanne, create invoices & purchase invoices, record payments/incomings, post balanced journal entries, stage bank payment orders, and process the PSD2 bank worklist. Run `simplbooks help`.
Stateful — run `simplbooks doctor` first.
