# whatsapp

WhatsApp CLI over a self-hosted WAHA bridge — list chats, read/export a chat's history to JSON with voice/audio + photos, and transcribe voice notes. Run `whatsapp help`.
Run `whatsapp doctor` first to confirm the instance is reachable and the session is linked.
