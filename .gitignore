# Secrets — real credential/env files (the .example variants ARE tracked)
credentials.env
.env
.env.local
.env.*.local
.env.signoz.*
*.local

# OS / editor cruft
.DS_Store
*.swp

# Python build artifacts (uv runs the CLIs; no bytecode is tracked)
__pycache__/
*.pyc
