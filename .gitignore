# Secrets — real credential/env files (the .example variants ARE tracked)
credentials.env
.env
.env.local
.env.*.local
*.local

# Private working notes — grounded scratch (backlog, plans, session recaps).
# Deliberately reference the cross-project conversations that produced them;
# kept local so the public repo never ships those specifics.
.assets/
.claude/askproject/

# OS / editor cruft
.DS_Store
*.swp

# Python build artifacts (uv runs the CLIs; no bytecode is tracked)
__pycache__/
*.pyc
